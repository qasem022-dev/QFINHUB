# Stealth Browser Rebuild — Diagnosis & Plan
**Date:** May 19, 2026  
**Target:** `~/.hermes/tools/stealth_browser.py` (1972 lines → rebuilt)

---

## 🔍 DIAGNOSIS — Why X.com Detects the Current System

### Critical Failures (Causing 400 Rejection on `onboarding/task.json`)

| # | Vector | Current State | Why It Fails |
|---|--------|--------------|--------------|
| **C1** | `chromium_sandbox=False` | Line 1565 | Changes browser security model — detectable via `navigator` introspection |
| **C2** | `navigator.webdriver` | Set to `undefined` via getter | Real Chrome has NO `webdriver` property. Must DELETE entirely, not set to undefined. |
| **C3** | `--disable-blink-features=AutomationControlled` | Only one blink feature disabled | Need full suite: `AutomationControlled,ScriptedPrintBrowser,InterestCohortAPI` |
| **C4** | Platform spoofing | Linux spoofing `Win32` | X.com checks WebGL renderer + font availability against claimed platform |
| **C5** | TLS fingerprint | Stock Playwright Chromium | Different TLS cipher suite than real Chrome — Cloudflare detects this |
| **C6** | CDP detection | No CDP runtime detection prevention | `navigator.webdriver` patched but CDP connection exposes debugger |

### Secondary Failures

| # | Vector | Issue |
|---|--------|-------|
| S1 | Timezone patch | `getTimezoneOffset()` returns static value — breaks DST logic |
| S2 | Canvas noise | Only 0.1% — too predictable, no per-pixel variance |
| S3 | `chrome.runtime` | Missing `chrome.runtime.id` |
| S4 | `Notification.permission` | Not spoofed |
| S5 | `window.outerWidth/Height` | Not spoofed — differs from inner in automation |
| S6 | Init script timing | `add_init_script` doesn't guarantee execution before site JS |
| S7 | Mouse behavior | `StealthBrowser.click()` doesn't use `HumanBehavior` — teleports mouse |
| S8 | `--disable-automation` | Conflicts with Playwright's `--enable-automation` default flag |

---

## 🔧 REBUILD PLAN

### Phase 1: Browser Launch Hardening (Lines 1479-1567)
1. Remove `chromium_sandbox=False` → use `--no-sandbox` only (different thing)
2. Add full blink feature disable list
3. Add `--disable-features=OptimizationGuideModelDownloading,OptimizationHintsFetching`
4. Add `--disable-field-trial-config`, `--disable-hang-monitor`
5. Remove `--disable-automation` → it conflicts with Playwright defaults; instead patch via init script
6. Set `--use-angle=swiftshader` for consistent WebGL
7. Set `--disable-gpu-sandbox`, `--disable-software-rasterizer`

### Phase 2: Fingerprint Script Rebuild (Lines 60-478)
1. **DELETE `navigator.webdriver`** — not set to undefined
2. Fix timezone: use actual Date calculation, don't override statically
3. Canvas noise: increase to 0.5-1.5% with per-pixel Gaussian variance
4. Add `Notification.permission = 'default'`
5. Add `window.outerWidth/Height` matching inner dimensions
6. Add `chrome.runtime.id` with realistic GUID
7. Add frame/modal detection cloaking
8. Add `navigator.mediaCapabilities` spoofing
9. Add `SharedWorker` detection prevention
10. Execute script BEFORE page load via `page.route()` interceptor

### Phase 3: Human Behavior Enhancement (Lines 498-650)
1. Make `StealthBrowser.click()` route through `HumanBehavior.move_mouse()` + Bezier
2. Add scroll with variable speed + reading pauses (already exists, verify)
3. Add `random_jitter()` — micro-movements during idle
4. Add `human_hover()` — hover before clicking (natural behavior)

### Phase 4: Advanced Eye v2 (Lines 650-950 area)
1. Add X.com-specific challenge detection patterns
2. Add `onboarding/task.json` 400 response monitoring
3. Add Cloudflare Turnstile detection
4. Add rate-limit backoff logic

### Phase 5: CDP/Proxy Hardening
1. Fix `connect_over_cdp` to properly manage browser contexts
2. Add proxy health checking before use
3. Add per-domain proxy affinity

### Phase 6: Testing
1. Test on X.com login flow
2. Test on httpbin.org/headers (fingerprint verification)
3. Test on browserleaks.com (WebGL, canvas, font checks)

---

## Files to Change
- `~/.hermes/tools/stealth_browser.py` — primary rebuild
- `~/.hermes/skills/stealth-browser/SKILL.md` — update docs

## Verification
1. `navigator.webdriver` must not exist (not undefined, DELETED)
2. X.com login → `onboarding/task.json` returns 200 (not 400)
3. httpbin.org/headers shows correct UA + sec-ch-ua
4. browserleaks.com/canvas shows randomized hash
5. browserleaks.com/webgl shows spoofed vendor/renderer
